// function importAll() {
//   const r = {"pic":"./images/lt_flag.png"}
//     let images = {};
//     Object.keys(r).map(
//       (item) => {
//          images[item.replace('./', '')] = item; 
//         }
//       );

//     return images;
//   }
  
//   const imagest = importAll();
  
//  console.log(imagest);

// function importAll(r) {
//   return r.keys().map(r);
// }

// const images = importAll(require.context('./', false, /\.(png|jpe?g|svg)$/));